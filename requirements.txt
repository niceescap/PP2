# PaceParser2 — Linux/Termux Requirements
# Python 3.8+ required for static file serving
# No pip packages needed — use stdlib http.server
#
# Termux install:
#   pkg install nodejs python
#
# Project setup:
#   npm install
#   npm run build
#   python3 -m http.server 8080 --directory dist
#
# Or dev mode (hot reload):
#   npm run dev
